TASK DESCRIPTION:

Imagine you're a system administrator tasked with monitoring the performance of
several servers running GNU+Linux in a data center. To effectively track CPU usage,
memory consumption, disk I/O, and network statistics. The node-exporter package is
already downloaded for you at /home/ec2-user/node_exporter.tar.gz. Your goal is to
place the binary in the right path, and create a systemd service to run it all the time.

------------------------------------------------------------------------------------------

COMMANDS USED :
---------------

tar -xzf node_exporter.tar.gz

sudo mv node_exporter-1.9.1.linux-amd64/node_exporter /usr/local/bin/


OUTPUT :
--------
ubuntu@ip-10-0-17-84:~$ ls /usr/local/bin/
aws  aws_completer  cpu_hog.py  node_exporter



CREATING THE USER :
-------------------
sudo useradd --no-create-home --shell /bin/false nodeusr

WRITING THE SERVICE:
--------------------

sudo vim /etc/systemd/system/node_exporter.service

and the following were written in it:

[Unit]
Description=Node Exporter
After=network.target

[Service]
User=nodeusr
ExecStart=/usr/local/bin/node_exporter
Restart=always

[Install]
WantedBy=multi-user.target



RUNNING IT:
-----------

sudo systemctl daemon-reload
sudo systemctl start node_exporter
sudo systemctl enable node_exporter 

I GOT:
------
Created symlink /etc/systemd/system/multi-user.target.wants/node_exporter.service → /etc/systemd/system/node_exporter.service.

FINAL CHECK:
------------
sudo systemctl status node_exporter
